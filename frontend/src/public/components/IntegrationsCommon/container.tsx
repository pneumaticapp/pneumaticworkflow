import { IntegrationsCommon } from './IntegrationsCommon';

/**
 * @deprecated Use IntegrationsCommon directly — it uses
 * useSelector/useDispatch internally. This alias exists
 * for backward compatibility with existing imports.
 */
export const IntegrationsCommonContainer = IntegrationsCommon;
