import { IWorkflow } from "../../../../types/workflow";

export type TableColumns = { 
  workflow: IWorkflow,
  starter: IWorkflow,
  progress: IWorkflow,
  step: IWorkflow,
  performer: IWorkflow,
};
